using System;

class Program
{
    static void Main()
    {
        for (int i = 1; i <= 100; i++)
        {
            if (i % 3 == 0 || i % 5 == 0)
            {
                continue;
            }
            Console.WriteLine(i);
        }
    }
}